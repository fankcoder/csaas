# QA / 待确认问题

1. 前端目录：提示词写的是 `csweb\`，当前仓库已有 `cs_web\`，本轮实现采用 `cs_web\`，避免生成重复前端项目。
   答：我提示词写错了，你理解的没问题就是cs_web目录
2. YouPin/悠悠有品：样例 `Price` 模型里没有 youpin 价格、销量、时间字段，当前套利计算暂未纳入 `www.youpin898.com`。需要确认字段名，或扩展模型。
    答：uu_price就是悠悠有品的价格字段，销量和时间没有，需要拓展
3. ShadowPay 手续费：提示词只明确 Waxpeer `7%`、BUFF `2.5%`，当前 ShadowPay 暂按 `5%` 可配置手续费处理，需要确认真实费率。
    答：是的，以后没有的东西都按5%手手续费等我最后手动统一配置即可。
4. 真实数据源：Waxpeer、ShadowPay、BUFF、YouPin 的抓取方式、接口凭证、频率限制、代理策略待确认。
   答：目前无需考虑，保留空即可。
5. 支付系统：当前实现为套餐和模拟支付订单，需要确认正式支付渠道，例如微信支付、支付宝、Stripe 或 Paddle。
    答：目前版本用模拟支付即可，可以海外用户可以提供支持数字货币支付。
6. 会员权限：当前套利榜单暂不强制登录，后续需要确认免费用户、付费用户、管理员的数据可见范围。
   答：你能否按照海外流行的saas平台设计，哪些需要登录，哪些不需要登录或者免费用户可以查看哪些范围，你来帮我设计。
   实现：公开访问官网、套餐、注册登录、发送邮箱验证码和市场概览；登录用户可进入个人中心、绑定 Steam、查询自己的 Steam 库存和基础饰品数据；订阅用户或 staff/superuser 可访问套利利润分页查询和单饰品套利详情。开发账号 `devadmin` 是最高权限账号。

7. 价格提醒：需要确认通知渠道和执行策略。
   待补充：
   - 是否需要站内消息、邮件、Telegram、企业微信、Discord、短信中的哪些渠道？
   - 免费用户/订阅用户分别允许多少条提醒规则？
   - 提醒触发后是否需要冷却时间，例如同一规则 30 分钟、1 小时或 24 小时内只提醒一次？
   - 触发条件是否只支持利润率/利润金额/方向 B 平台/销量，还是还要支持价格低于指定值、指定饰品补货？
   - 是否需要后台定时任务自动扫描提醒？如果需要，使用 Celery + Redis 还是 Django management command + 系统计划任务？

8. 价格稳定性评分：当前没有历史价格快照表，无法严谨计算价格波动稳定性。
   待补充：
   - 是否保存每次抓取的历史价格快照？
   - 历史保留多久：7 天、30 天、90 天还是永久？
   - 稳定性评分按价格标准差、最大回撤、连续可套利时长，还是成交成功率计算？
   - 价格快照表数据量会明显增长，是否接受按天归档或只保留聚合指标？

9. Steam 第三方登录：当前项目改为 Steam OpenID 登录方式，对齐 `cs_backend/tmp/authentication` 里参考项目的思路。
   当前实现：后端生成 Steam OpenID 登录 URL，Steam 回调到 `/api/auth/steam/callback/` 后用 `check_authentication` 校验，再重定向到前端 `/steam/callback?token=...`，前端保存站内 token。
   配置项：
   - `STEAM_API_KEY`：可选，用于登录成功后调用 `GetPlayerSummaries` 拉取公开 Steam 昵称等资料。
   - `STEAM_OPENID_REALM`：建议与后端域名一致，例如 `http://127.0.0.1:8000`。
   - `STEAM_OPENID_RETURN_TO`：后端回调地址，例如 `http://127.0.0.1:8000/api/auth/steam/callback/`。
   说明：Steamworks OAuth 2.0 仍然需要 Valve 分配 `client_id`，但当前网页登录不走那套方案，因此不再需要 `STEAM_OAUTH_CLIENT_ID` 和 `STEAM_OAUTH_REDIRECT_URI`。
